<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <!-- Course title displayed with page title -->
  <h1>{{title}}: {{course.title}}</h1>

  <!-- Booking form for student -->
  <form action="/book-course/{{course._id}}" method="POST">
    <!-- Student name input -->
    <div class="form-group">
      <label>Your Name</label>
      <input type="text" class="form-control" name="studentName" required>
    </div>

    <!-- Student email input -->
    <div class="form-group">
      <label>Your Email</label>
      <input type="email" class="form-control" name="studentEmail" required>
    </div>

    <!-- Submit booking -->
    <button type="submit" class="btn btn-success">Book Now</button>
  </form>

  <!-- Link to cancel and go back to course details -->
  <a href="/courses/{{course._id}}" class="btn btn-secondary mt-2">Cancel</a>
</div>
</body>
</html>

