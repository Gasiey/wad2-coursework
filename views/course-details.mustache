<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <!-- Course title -->
  <h1>{{course.title}}</h1>

  <!-- Course details -->
  <p>{{course.description}}</p>
  <p><strong>Date & Time:</strong> {{dateTimeFormatted}}</p>
  <p><strong>Location:</strong> {{course.location}}</p>
  <p><strong>Duration:</strong> {{course.duration}}</p>
  <p><strong>Price:</strong> {{course.price}}</p>

  <!-- Public button to book the course -->
  <a href="/book-course/{{course._id}}" class="btn btn-success">Book Course</a>

  <!-- Extra options for logged-in organisers -->
  {{#organiser}}
    <a href="/edit-course/{{course._id}}" class="btn btn-warning">Edit Course</a>
    <a href="/delete-course/{{course._id}}" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this course?');">Delete Course</a>
    <a href="/participants/{{course._id}}" class="btn btn-secondary">View Participants</a>
  {{/organiser}}

  <!-- Button to return to homepage -->
  <a href="/" class="btn btn-primary">Back to Courses</a>
</div>
</body>
</html>

