<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <!-- Page title -->
  <h1>{{title}}</h1>

  <!-- Loop through bookings if any exist -->
  {{#bookings}}
   <div class="card mb-3 p-3">
     <!-- Show booking ID -->
     <h5><strong>Booking ID:</strong> {{_id}}</h5>
     <!-- Show course title for this booking -->
     <p><strong>Course:</strong> {{courseTitle}}</p>
     <!-- Show who booked -->
     <p><strong>Student Name:</strong> {{studentName}}</p>
     <p><strong>Student Email:</strong> {{studentEmail}}</p>
   </div>
  {{/bookings}}

  <!-- If no bookings exist, show this message -->
  {{^bookings}}
    <p>No bookings yet.</p>
  {{/bookings}}

  <!-- Back button to homepage -->
  <a href="/" class="btn btn-primary">Back to Courses</a>
</div>
</body>
</html>
